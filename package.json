{
  "name": "omp-notify-feishu",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check": "bun test && tsc --noEmit"
  },
  "omp": {
    "extensions": [
      "./index.ts"
    ]
  },
  "devDependencies": {
    "@oh-my-pi/pi-coding-agent": "17.3.4",
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
